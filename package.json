{
  "name": "lab-5",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "palywright test",
    "test:report": "playwright test && allure generate allure-results --clean -o allure-report",
    "test:smoke": "npx playwright test --grep @smoke",
    "test:regression": "npx playwright test --grep @regression",
    "test:all": "npx playwright test --reporter=line,allure-playwright,html",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.9.0",
    "allure-playwright": "^3.4.1"
  },
  "dependencies": {
    "@faker-js/faker": "^9.9.0"
  }
}
